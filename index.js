const Group = require('./lib/group');
const DCPlatform = require('./lib/platform');
const CommandGroup = require('./lib/commandGroup');

module.exports = {
    DCPlatform: DCPlatform,
    Group: Group,
    CommandGroup: CommandGroup
}